version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
    reviewers:
      - "arminwitte"
    # Monitor both pyproject.toml and uv.lock
    allow:
      - dependency-type: "all"
