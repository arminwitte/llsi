System identification library. Engineering-focused, lightweight, easy to use.

- Lint: `uv run ruff check . && ruff format --check .`
- Test: `uv run pytest`
- Always run lint before commit
- Python 3.9-3.13 supported
